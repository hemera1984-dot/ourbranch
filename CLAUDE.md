# 하랑지점(ourbranch) — 프로젝트 헌법

신한라이프 지점 단위 팀 관리 플랫폼. 마이가디언 v2에서 우리팀 기능을 분리해
지점 전체(팀 여러 개)로 확장한 독립 프로젝트다. 이 문서의 원칙은 모든 작업에 우선한다.

## 지향점

- 지점의 모든 구성원이 함께 쓰는 공유 도구다. 어떤 기기에서든 열람·입력이 가능해야 한다.
- 정적 JSON이 아니라 서버 동기화가 기본이다 — 공지·일정·출근을 전원이 실시간 공유하는 것이
  이 프로젝트가 존재하는 이유다.
- 지점명은 계속 바뀔 수 있다. 「하랑지점」 등 지점명을 코드에 박지 않는다 —
  서버 settings의 지점명 하나만 고치면 되는 구조를 유지한다.

## 조직·권한 (2026-07-31 사용자 확정)

- 직급(2026-08-02 사용자 확인 — 마이가디언 서버 GRADES와 동일):
  BM 지점장 > ESL 부지점장 > SSL 팀장 > GSL 부팀장 > FC 팀원.
  화면의 직급 목록은 web/index.html의 GRADES 상수 한 곳에서만 만든다(선택칸·정렬 공용).
- **팀의 단위는 부지점장이다.** 부지점장 밑에 소속된 사람들이 한 팀이고,
  팀장·부팀장은 팀 안의 직급일 뿐이다.
- 권한:
  - 총관리자(안창민): 관리자 임명·해제, 전 기능.
  - 관리자(부지점장 이상 기본, 총관리자가 임명·회수 가능): 팀원 추가·삭제, 조직도 수정.
  - 열람 범위 — 총관리자·지점장: 지점 전체 / 부지점장: 자기 팀
    (총관리자가 승인한 부지점장만 전체 열람) / 팀원: 자기 팀.
- 데이터(공지·일정·출근·과제·TA일지·업적)는 팀 단위로 가른다. 지점 공통 공지는 전원 열람.

## 인증·서버

- 인증은 마이가디언과 공유한다: NCP 서버 `49.50.136.14`, `api.insurguard.life`,
  구글 OAuth + 총관리자 승인. **팀원이 프로그램마다 따로 승인받게 하지 않는다.**
- 방식: ourbranch 서버는 같은 NCP 인스턴스에서 마이가디언 세션 DB(myguardian.db)를
  읽기 전용으로 조회해 세션 토큰을 검증한다. 자체 데이터는 별도 파일(ourbranch.db).
- 서버 코드는 마이가디언 방식 승계: node:http + node:sqlite, npm 의존성 0.
- 배포: 웹은 GitHub Pages + `branch.insurguard.life`(DNS 추가 필요), API는 Caddy 리버스 프록시.
- API 키·비밀번호는 채팅에 절대 붙여넣지 않는다 — 파일로 전달, 길이 검증 후 삭제.
  서버 전달 시 윈도우 개행(\r\n) 함정 주의.

## 언어 (최우선 규칙)

- 모든 응답·보고·커밋 메시지·문서는 한국어로 작성한다. 첫 문장부터 한국어다.
- 하위 에이전트를 띄울 때도 "응답은 한국어"를 명시한다.
- 영어는 코드·명령어·파일명·기술 고유명사에만 허용한다.

## 일정 화면 불변 원칙 (2026-08-01 사용자 재확인 — 반복 강조 항목)

**일정 추가는 진짜 간단하고 쉽고 편리할 것. 읽을 때 가독성이 좋을 것.**
일정 화면을 고칠 때마다 이 두 가지로 먼저 검증한다. 기능을 늘리다가 입력 단계가
늘어나거나 글자가 작아지면 그 변경은 되돌린다.

- 입력: 날짜·대상은 화면이 정해준다(칸 클릭·시트의 추가 버튼). 사용자는 구분 칩 하나,
  시간, 내용, 장소만 채운다.
- 열람: 달력은 요약(색 점·시간·제목), 상세는 눌러서 큰 화면(우측 패널·모바일 시트)으로.
- 범위: 지점장·총관리자 기본은 부지점장 일정만 — 전 팀원이 한 번에 뜨면 정신없다.
  팀 전체·내 일정으로 전환 가능. 팀원·부지점장 기본은 팀 전체.

## 입력 원칙 (2026-08-02 사용자 확정)

**「치는 것」을 「고르는 것·기억하는 것」으로 바꾼다.** 칸 수를 줄이는 게 아니라
(여러 가지가 입력돼야 한다) 한 칸을 채우는 데 드는 동작을 줄인다.

- 값이 정해진 칸은 타이핑하지 않는다 — 그리드 열에 `opts`를 주면 칸을 누를 때
  칩이 뜬다. 타이핑도 그대로 된다(엑셀 붙여넣기를 막지 않는다).
- 하루 종일 같은 값을 다시 치지 않는다 — 그리드 열의 `sticky`, 일정의 구분·시간,
  일일보고의 출근 상태를 기억해 다음 입력에 미리 채운다.
- **미리 채우되 자동으로 저장하지 않는다.** 「지난 보고 불러오기」는 누른 사람만
  가져간다 — 엉뚱한 내용이 저절로 저장되면 신뢰를 잃는다.
- 기억은 브라우저(localStorage)에 둔다. 사람마다 기기마다 다르다.
- AI가 값을 지어내지 않는다(「AI 자동 배치·AI 요약 금지」와 같은 선).

## 일정 구분과 세부 항목 (2026-08-03 사용자 확정)

부지점장이 팀 일정으로 한 달치를 미리 넣는 것들이다. 구분마다 필요한 항목이 다르므로
`KIND_FORM` 한 곳에서만 정한다 — 여기만 고치면 팝업 칸·제목이 함께 바뀐다.

구분 목록은 **현장에서 쓰는 말 그대로** 둔다 (2026-08-03 부지점장 확인):
밸류업/이관 · 외근 · 내근 · TS1 · TS2 · 시험 · 입과전교육생일정 · 입과교육 ·
차월교육 · 교육 · 지점일정 · 본부일정 · 회의 · 마감 · 휴가 · 기타.
「상담」은 외근에 들어가므로 「밸류업/이관」으로 바꿨다.

| 구분 | 받는 것 |
|---|---|
| TS1 · TS2 | 면접관, 장소 |
| 시험 | 대상, 장소 |
| 입과전교육생일정 · 입과교육 | 교육 대상, 장소 |
| 차월교육 | 차월(숫자), 교육 대상, 장소 |

- **예전 구분(상담·강의·면접·GROW)은 목록에서만 뺐다.** 색·형식은 그대로 읽는다(OLD_KINDS) —
  지난 기록을 깨뜨리지 않기 위해서다. 구분을 바꿀 때 이 원칙을 지킨다.

- **사람은 치지 않고 명단에서 고른다.** 이메일로 저장하므로 개명·동명이인에 흔들리지 않는다.
- 세부는 `events.detail`(JSON)에 담고, 목록에 뜰 제목은 `composeTitle`이 만든다 —
  화면 어디서나 한 줄로 읽히게. 제목을 손으로 쓰지 않는다.
- 한 달치를 넣는 길이 둘이다.
  ① **반복**(매주·격주·매일·매월) — 같은 내용이 되풀이될 때. 세부가 모든 회차에 붙는다.
  ② **빠른 입력 표** — **면접관처럼 회차마다 값이 다를 때.** 여러 줄을 채우고 한 번에 저장한다.
     사람 칸은 눌러서 여럿 고른다(칩). 엑셀 붙여넣기도 그대로 된다.
  면접관은 매번 바뀌므로 ②가 기본이다(2026-08-03 사용자).
- 면접 일정에 **후보자 이름은 넣지 않는다.** 후보자 개인정보는 TA 일지 한 곳에만 둔다
  (거기만 비밀번호·열람 기록·보관기간이 걸려 있다).

## 디자인 — 가독성·편의성 우선 원칙 (2026-08-01 사용자 확정)

이 프로그램은 혼자 쓰는 도구가 아니라 **전 지점원(연령대 다양, 수십 명)이 쓰는 도구**다.
색감과 글씨체는 장식이 아니라 사용성이다. 모든 화면 판단은 디자인·가독성·편의성 순의
우선순위로 검증한다. 그래프든 표든 일정·현황 확인은 가독성이 최우선이다.

- 의미 있는 텍스트의 최소 크기는 12px (본문 14px). 10~11px은 장식성 라벨(eyebrow)에만.
- 보조 텍스트도 대비를 확보한다 — 연회색 글씨로 정보를 흐리게 만들지 않는다.
- 다크모드는 중성 검정 계열 (푸른 남색 금지 — 눈 피로).
- 표는 오늘 열 강조·첫 열 고정·줄바꿈 허용으로 "누가·언제"를 잃지 않게 한다.
- 글래스모피즘 등 가독성을 깎는 장식 스타일은 쓰지 않는다. 배치 개선(벤토 그리드 등)은 허용.
- 카드·타일 상단의 장식용 포인트 컬러 스트립 금지 (2026-08-01 사용자: "AI 틱하다").
  색은 상태 신호(선택 막대·상태 배지·구분 칩)에만 쓴다. 브랜드 라인(사이드바 상단 시안)은 예외.

## 디자인

- 마이가디언 패밀리 디자인 언어 승계 (마이가디언 v2 CLAUDE.md 디자인 절 참조):
  - 네이비 `#102a4e` 사이드바 + 시안 `#00a6c7` 라인, 캔버스 `#f5f7fa`, 포인트 블루 `#0758d6`.
  - Pretendard 단일 서체, 위계는 크기·굵기·색으로만.
  - 면 구분은 1px 선, radius 6/4/3, 모션은 0.14초 색 전환만, 그라데이션 금지.
  - 선택 표시는 좌측 3px 인셋 막대. 화면 최상단은 eyebrow → 제목 → 설명 3단.
- 이모지·AI 말투·은유 금지. 장식 색 금지, 색은 신호로만.
- 가짜 데이터로 화면 채우기 금지 — 실데이터 없으면 구획 생략.
- 기준 기기: ① 데스크톱 1920px ② 태블릿 1024~1366px ③ 모바일 390px.
- **주 작업 기기는 노트북·태블릿이다**(2026-08-03 사용자). 폰으로는 일정 조회·일정 추가·
  일일보고 정도를 자주 한다. 나머지 기능도 폰에서 다 되지만, 이 셋만 폰이 기준이다 —
  고칠 때 390px에서 먼저 확인한다.
- **손가락 크기는 폭이 아니라 포인터로 가른다** — `@media (pointer: coarse)`.
  태블릿은 1024px이라 폭으로만 가르면 27px 줄을 받는다. 최소 44px.
  13px 라디오 같은 것을 두지 않는다(.pickchip을 쓴다).
- 일일보고의 팀 표는 좁은 화면에서 접어 둔다 — 폰으로 여는 이유는 대개 내 보고를 쓰려는 것이다.

## 조직도·내 정보 (2026-08-02 사용자 지시)

- **조직도는 지점 전체가 다 보인다.** 팀·구성원 명단은 누구에게나 열려 있다.
  가리는 것은 자료(일정·업적·TA 일지·공지)이고, 그건 각 엔드포인트가 팀 단위로 막는다.
  「명단이 보인다」와 「자료를 본다」를 섞지 않는다.
- 구글 계정 이름이 그대로 들어오면 "kim jia"처럼 남는다. 승인 후 첫 접속에서
  「내 정보」를 띄워 본인이 실명을 확인하게 한다 (members.profile_done).
- **본인이 고칠 수 있는 것은 이름·휴대폰뿐이다.** 팀·직급은 조직도에서 관리자가 정한다 —
  직급이 관리자 권한의 근거라서(GRADE_OF_ROLE) 본인이 올리면 권한 상승이 된다.
- 명부 항목(2026-08-02 추가): 휴대폰·생일·위촉일. 지점원끼리 공유한다 —
  조직도에서 사람을 누르면 연락처 카드가 뜬다. 본인이 「내 정보」에서 넣는다.
- **생일은 MM-DD만 받는다.** 연도를 받으면 나이가 지점 전체에 공개된다.
  홈에 「지점 생일」 타일로 2주 앞까지 D-day 표시.

## 리쿠르팅 (2026-08-02 사용자 지시로 분리)

TA 일지·도입 현황은 「업무」가 아니라 **「리쿠르팅」** 묶음이다. 성격이 다르다.

- TA 일지의 「단계」 칸이 도입 현황의 재료다 — 통화 → 면접 → 위촉(+거절).
  빈 값은 통화로 본다(일지를 쓴 것 자체가 통화다).
- **도입 현황(`GET /recruit`)은 숫자만 돌려준다.** 후보자 이름·연락처가 없으므로
  TA 잠금과 무관하게 열린다. 이 경계를 흐리지 않는다 — 집계 화면에 이름을 넣지 않는다.
- 「내가 도입한 팀원」은 조직도의 도입자(members.recruiter_email)에서 온다.

## 조직도는 피라미드다 (2026-08-03 사용자 확정)

보험 조직은 팀 안에서 **누가 누구를 도입했나**로 또 줄기가 갈린다(계파).
조직도는 그 줄기를 그대로 그린다 — 보기를 따로 두지 않는다. 조직도가 곧 피라미드다.

- 꼭대기는 부지점장. 그 밑으로 도입 계보가 상자로 쌓인다(members.recruiter_email).
- **팀 밖(대개 지점장)에서 뽑혀 온 사람은 부지점장 옆줄에 선다** —
  지점장 직도인데 부지점장이 안 된 사람이 여기 온다.
- 도입자를 아직 안 정한 사람은 부지점장 밑에 붙는다.
- **단계를 「직도·간도」로 부르지 않는다.** 세 단계부터 부를 말이 없어진다. 모양이 곧 계보다.
- 고치는 길 둘:
  ① 상자의 **「+」** — 그 사람이 뽑은 사람으로 새로 만든다. 도입자를 빼먹지 않게.
  ② **끌어서 놓기** — 사람 위에 놓으면 그 사람이 도입자가 된다(계파 이동).
     자기 아래 사람 밑으로는 못 옮긴다(줄기가 끊긴다).
  폰에서는 끌기가 안 되므로 팀원 창의 「도입자」 선택이 대안이다.
- 드래그는 더 이상 순서를 바꾸지 않는다. 피라미드에서는 순서보다 「누구 밑인가」가 먼저다.

## 조직도 자리·순서 (2026-08-02 사용자 지시)

- **계정 연결에는 길이 두 개다.** 새로 들어오는 사람은 초대 문구 → 로그인 → 승인.
  이미 로그인한 사람(총관리자 본인처럼 자리와 계정이 따로 노는 경우)은
  조직도에서 그 자리를 눌러 계정을 고른다(`POST /admin/members/link`).
  자리의 팀·직급·도입자와 그동안의 기록이 계정으로 옮겨가고 빈 자리는 사라진다.
- 붙일 계정은 **마이가디언에 실제로 로그인한 적이 있어야 한다.** 아무 주소나 받으면
  아무도 못 들어오는 유령 자리가 생긴다.
- **조직도 순서는 손으로 정한다**(members.sort_order). 끌어서 놓거나, 팀원 창의
  「위로/아래로」로 옮긴다 — 손가락으로는 끌기가 안 되므로 두 길을 함께 둔다.
  아직 정하지 않은 사람은 종전대로 직급·이름 순으로 뒤에 붙는다.
- 순서 바꾸기·계정 연결은 관리자 이상, 자기 팀만. 다른 팀 목록에 놓으면 팀이 바뀌는데
  이때는 **양쪽 팀에 대한 쓰기 권한**이 있어야 한다.

## 개인정보

- 실명·연락처 등 개인정보를 이 저장소에 저장하지 않는다. 개인정보는 서버 DB에만 둔다.
- 외부 반출 산출물에서 이름은 가운데 글자 마스킹(안*민), 전화번호는 중간 자리 마스킹.
- 도입 후보자 정보도 같은 규칙을 적용한다.
- TA 일지 열람 범위(2026-08-02 사용자 재지시로 변경): **지점 전체가 함께 본다.**
  알바몬 등에서 걸러야 할 상대를 지점이 공유하는 것이 이 일지의 주된 쓸모라서다 —
  팀별로 갈라두면 옆 팀이 이미 거른 사람에게 또 연락하게 된다.
  「주의」 칸에 표시하면 줄 전체가 붉게 뜨고, 「주의 명단」 버튼으로 기간과 무관하게 모아본다.
  쓰기(수정·삭제)는 그대로 작성자 본인과 관리자만.
  후보자 실명·연락처가 지점 전체에 열리므로 세 겹으로 막는다(2026-08-02 확정):
  ① **지점 공용 비밀번호** — 지점장·총관리자가 정하고, 한 번 풀면 8시간 유지.
     비밀번호를 바꾸면 열어둔 사람 전원이 다시 입력한다.
  ② **열람 기록**(ta_access) — 누가 언제 열었는지 남긴다. 지점장·총관리자만 조회.
     민원이 들어왔을 때 이 기록이 없으면 아무것도 확인할 수 없다.
  ③ **보관기간 6개월** — 서버가 켜질 때와 하루 한 번, 6개월 지난 일지와 열람 기록을 지운다.
  공용 비밀번호는 수십 명이 아는 값이라 그 자체로 강한 보호가 아니다.
  실제 방어는 ②열람 기록과 ③보관기간이다. 비밀번호를 이유로 이 둘을 빼지 않는다.

## 동명이인 원칙 (2026-08-02 사용자 지시)

지점에 동명이인이 실제로 있다. **기록의 주인은 이름이 아니라 이메일(계정)로 판정한다.**

- ta_logs.author_email, perf.member_email, perf_goals.member_email이 소유자다.
  author·member(이름)는 표시용 스냅숏일 뿐이다.
- 화면은 같은 이름이 둘 이상이면 구분 표식을 붙인다 (memberLabel) —
  팀 → 팀·직급 → 계정 순으로 좁혀 실제로 유일해지는 표식을 고른다.
- 이메일 없이 등록해도 고유 키를 만들어 서로 덮어쓰지 않는다.
- 이름을 고쳐도 과거 기록의 소유가 바뀌지 않는다.

## 세션 운영

- 작업 시작 전 `docs/decisions.md`를 읽는다.
- 세션 종료 전 변경·검증 결과·미완료를 기록하고 commit·push한다.
- 로직에 패치 위에 패치를 쌓지 않는다. 더러워지면 다시 짓는다.

## 구조

```
web/      # 정적 사이트 (GitHub Pages 배포)
server/   # 지점 API (node:http + node:sqlite, NCP 배포)
docs/     # 결정 기록·설계
```
