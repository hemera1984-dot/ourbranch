# 하랑지점 — 코덱스 작업 안내

## 헌법은 CLAUDE.md 하나다

이 저장소의 규칙은 **`CLAUDE.md` 전문**이다. 작업 전에 읽고 그대로 따른다.
**이 파일에 규칙을 옮겨 적지 않는다** — 복사본은 반드시 원본과 어긋난다.

## 역할

- Claude Code = 주 시공자
- Codex = 검수·검증 전담. 사용자가 요청하면 시안 경쟁의 별도 안을 낸다.
- 머지 권한은 사용자에게 있다.

## 이 저장소에서 특히 조심할 것

- 지점 단위로 여러 사람이 쓰는 물건이다. **한 사람 PC에서만 되는 기능은 미완성**이다.
- 고객 실명·연락처는 저장소에 남기지 않는다. 팀 공유 화면에는 마스킹 표기만 내보낸다.

## 작업 전

1. `git status --short`, 현재 브랜치, 최근 커밋을 확인한다.
2. `CLAUDE.md` 전문을 읽는다.
3. `HANDOFF.md`가 있으면 읽는다.

## 작업 후

저장소 루트 `HANDOFF.md`에 **무엇을 했고 · 무엇을 검증했고 · 무엇이 남았는지** 남기고
commit·push 한다. 없으면 만든다. 다음에 붙는 도구가 이 파일로 맥락을 이어받는다.
